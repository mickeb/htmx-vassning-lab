# htmx-vassning-lab

```bash
docker compose up        # start the lab
docker compose down      # stop it
docker compose down -v   # stop it and delete the containers, volumes and data
```

<http://localhost:4000/exercises/>
