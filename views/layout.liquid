<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ title | default: "HTMX vassning — lab" }}</title>
  <link rel="stylesheet" href="/css/app.css">
</head>
<body>
  {% block content %}{% endblock %}

  <script src="/js/app.js"></script>
  {% if dev %}<script src="/js/dev-reload.js"></script>{% endif %}
</body>
</html>
